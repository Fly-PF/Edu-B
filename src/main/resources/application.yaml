server:
  port: 8080
spring:
  application:
    name: Edu-B
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 1GB
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    driver-class-name: ${edu.datasource.driver-class-name}
    username: ${edu.datasource.username}
    password: ${edu.datasource.password}
    url: jdbc:mysql://${edu.datasource.host}:${edu.datasource.port}/${edu.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
  data:
    redis:
      host: ${edu.redis.host}
      port: ${edu.redis.port}
      password: ${edu.redis.password}
      database: ${edu.redis.database}
      connect-timeout: 10s
  mail:
    host: smtp.qq.com
    port: 465
    username: your@qq.com
    password: your-auth-code
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
logging:
  level:
    org.springframework.security: DEBUG
edu:
  ai:
    provider: ${EDU_AI_PROVIDER:mock}
    base-url: ${EDU_AI_BASE_URL:}
    api-key: ${EDU_AI_API_KEY:}
    model: ${EDU_AI_MODEL:}
    grading-model: ${EDU_AI_GRADING_MODEL:${EDU_AI_MODEL:}}
    lesson-model: ${EDU_AI_LESSON_MODEL:${EDU_AI_MODEL:}}
    grading-timeout-ms: ${EDU_AI_GRADING_TIMEOUT_MS:30000}
